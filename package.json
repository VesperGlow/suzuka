{
  "private": true,
  "scripts": {
    "build": "cargo build --release --locked --manifest-path ssg/Cargo.toml && rm -rf public && ./ssg/target/release/ssg build --source . --dest public && npx -y pagefind --site public"
  }
}
